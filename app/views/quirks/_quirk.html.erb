<div id="<%= dom_id quirk %>">
  <p class="my-5">
    <strong class="block font-medium mb-1">Title:</strong>
    <%= quirk.title %>
  </p>

  <p class="my-5">
    <strong class="block font-medium mb-1">Content:</strong>
    <%= quirk.content %>
  </p>

  <p class="my-5">
    <strong class="block font-medium mb-1">User:</strong>
    <%= quirk.user_id %>
  </p>

  <% if action_name != "show" %>
    <%= link_to "Show this quirk", user_path(@user), class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%
=begin%>
 <%= link_to 'Edit this quirk', user_quirk_edit_path(@user), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %> 
<%
=end%>
    <hr class="mt-6">
  <% end %>
</div>
