<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing quirk</h1>

  <%= render "form", quirk: @quirk %>

  <%= link_to "Show this quirk", user_quirk_path(@user, @quirk), class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to quirks", user_quirks_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>

